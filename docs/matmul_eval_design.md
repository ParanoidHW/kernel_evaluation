# MatMul 评估工具设计

本文件保留为兼容入口，主要内容使用中文维护。

详细设计请阅读：

- `matmul_eval_design_zh.md`

保留该文件名的原因是历史文档和脚本可能引用 `docs/matmul_eval_design.md`。后续更新 MatMul 设计时，应优先修改 `matmul_eval_design_zh.md`，必要时再在本入口补充摘要或迁移说明。
